spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:3307/spring_batch?characterEncoding=UTF-8&serverTimezone=UTC
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: root
  batch:
    jdbc:
      initialize-schema: never # 메타 데이터가 자동으로 생성되지 않도록 설정
